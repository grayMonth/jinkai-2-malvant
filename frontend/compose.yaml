services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      VITE_DATA_SOURCE: ${VITE_DATA_SOURCE:-mock}
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
